name: 🧠 Treinamento IA Lotofácil

on:
  # ▶️ EXECUÇÃO MANUAL (BOTÃO)
  workflow_dispatch:

  # ⏱️ EXECUÇÃO AUTOMÁTICA POR INTERVALO
  schedule:
    # Executa a cada 3 horas
    - cron: "0 */3 * * *"

  # 🔁 Opcional: ainda roda quando fizer push
  push:
    branches:
      - main
      - master

jobs:
  treinamento:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # 🔎 Garantir CSV
      - name: 🔎 Verificar CSV de concursos
        run: |
          if [ ! -f data/planilhas/Lotofácil.csv ]; then
            echo "❌ CSV não encontrado: data/planilhas/Lotofácil.csv"
            exit 1
          fi
          echo "✅ CSV encontrado"

      # 🧠 Executar laboratório / treinamento
      #   python -m src.engine.calibrador_elite
      - name: 🧠 Executar treinamento
        run: |
          python -m teste_treinamento

      # 📤 Commit apenas dos relatórios e memória
      - name: 📤 Commit e Push do relatório
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          git add src/reports/relatorios/*.txt || true
          git add src/memory/perfil_vencedor.json || true

          if ! git diff --cached --quiet; then
            git commit -m "📊 Relatório automático: $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "Nenhuma mudança detectada."
          fi
