name: 🧠 Treinamento IA Lotofácil

on:
  push:
    branches:
      - main
      - master

jobs:
  treinamento:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # 🔎 Etapa de segurança: garante que o CSV existe
      - name: 🔎 Verificar CSV de concursos
        run: |
          if [ ! -f data/planilhas/Lotofácil.csv ]; then
            echo "❌ ERRO: data/planilhas/Lotofácil.csv não encontrado!"
            exit 1
          else
            echo "✅ CSV encontrado"
          fi

      # 🗄️ IMPORTANTE: importar concursos ANTES do treino
      - name: 🗄️ Importar concursos e criar tabelas
        run: |
          python -m reimportar_concursos_corrigido

      - name: 🧠 Executar treinamento
        run: |
          python -m teste_treinamento

      # 📤 Commit SOMENTE do que importa
      - name: 📤 Commit e Push do relatório
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # Adiciona apenas relatórios (evita loop infinito)
          git add src/reports/relatorios/*.txt || true

          if ! git diff --cached --quiet; then
            git commit -m "📊 Relatório automático: $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "Nenhuma mudança detectada no relatório."
          fi
